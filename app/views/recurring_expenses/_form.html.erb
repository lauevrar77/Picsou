<%= form_with(model: recurring_expense) do |form| %>
  <% if recurring_expense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recurring_expense.errors.count, "error") %> prohibited this recurring_expense from being saved:</h2>

      <ul>
        <% recurring_expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :begin_date, style: "display: block" %>
    <%= form.date_field :begin_date %>
  </div>

  <div>
    <%= form.label :periodicity, style: "display: block" %>
    <%= form.text_field :periodicity %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
